# Legacy Scripts

These scripts have been replaced by the `ccr` CLI. Install with:

```bash
pip install -e .
ccr doctor
ccr start
```

The scripts are kept for reference but are no longer the primary interface.
